Alongside each passage's unchanged loss-bound claims, return operationalSupported and operationalReferences. Assess each passage's operational assertions in complete reply context against calculated.operationalReference and calculated.conditionalAssignment. A correct overall calculation or empty loss-bound claims does not establish operational accuracy.

operationalReferences must contain only applicable rule IDs from operationalReference.rules or conditionalAssignment. Use only ["not-applicable"] for a passage containing no operational assertion; set operationalSupported:true for that case. Otherwise cite at least one applicable reference. Set operationalSupported:false when the assertion contradicts the reference or cannot be established within its scope. A reference ID is evidence to check, not permission to assume a claim is true. Project these checks independently of the overall valid field.

Read each proposition literally with its stated conditions. Check whether ITM expiration is being equated with certain assignment, whether an assigned option is treated as still open, whether closing guarantees recovery of remaining time value, and whether broker policy or dividend exposure is invented. Distinguish before-assignment options from conditional after-assignment shares and surviving options. Physical-share delivery requires applicable verified terms or an explicit hypothetical, not an unknown or cash-settled contract. Do not silently supply a missing condition that changes the proposition. Conversely, explicit denials, qualified recovery, conditional inventory examples, and exercise-by-exception explanations acknowledging exceptions are permissible; do not require unrelated boilerplate.

Return every passage exactly once, retaining its original id and claims plus these two fields. Do not add private reasoning or explanatory prose. Keep valid as the overall review and project every required bound assertion independently.
