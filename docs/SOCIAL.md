# Build-in-public posts (up to 5 links count for the social prize)

Tag both every time: **X** `@lablabai @AlpacaHQ` · **LinkedIn** `lablab.ai`
and `Alpaca`. Attach the screenshot named in each post. Post the X version and
the LinkedIn version of the same message; submit the five strongest links.

---

**1 — Wed 2 Sep, after the first live fill (screenshot: ledger with
`order_result: filled`)**

X: Building ARGUS for the @lablabai × @AlpacaHQ hackathon. Every agent here
says "LLM proposes, code decides." Mine asks the next question: does the LLM
add anything? Same frozen shortlist, a quant and a Gemini both pick, I trade
the AI's pick on Alpaca paper and score both. First fill just landed. 🧵

LinkedIn: Same text, plus: "Deterministic code owns strikes, expiry, size and
max loss; the model can only choose or abstain. Sixteen risk gates, hash-bound
authorization, and the official Alpaca CLI reconciling positions after every
cycle. The number I'm after is AI minus quant after inference cost — published
whether it's positive, zero, or negative."

---

**2 — Wed evening (screenshot: the disagreement row on the board)**

X: First real disagreement. Quant: bearish SPY, because the 20-day trend is
down. Gemini: bullish QQQ, "bullish_bias, low_spread_pct", confidence 0.7.
Same data, opposite calls. Both get marked from the same quotes until exit.
This is the whole experiment in one row. @lablabai @AlpacaHQ

---

**3 — Thu morning (screenshot: scoreboard cards)**

X: After day one: [N] paired decisions, [K] where the AI changed the call.
AI minus quant after costs: [$X]. Evidence state: DESCRIPTIVE, because n is
tiny and I'd rather show the honest label than a Sharpe ratio. Setback of the
day: my flatten timer would have fired 5 minutes *after* the deadline. Fixed,
tested. @lablabai @AlpacaHQ

---

**4 — Thu afternoon (screenshot: governor veto with `no_duplicate_open_legs`)**

X: The governor vetoed the AI three cycles in a row today — it kept picking
the same QQQ spread it already held. Without that gate the account would have
stacked $28k of max loss by Friday. The paired score still counts the
decision; the broker just never sees it. @lablabai @AlpacaHQ

---

**5 — Fri, after flatten and submission (screenshot: final board + claim table
showing "AI beats quant: UNSUPPORTED")**

X: Submitted ARGUS. Final: [N] paired decisions on a $100k Alpaca paper
account, AI minus quant after inference [$X]. Claim "AI beats quant":
UNSUPPORTED — and that's the honest product. Every number reproduces from a
hash-chained ledger with one command. Repo + live board: [links].
@lablabai @AlpacaHQ

LinkedIn: add two sentences on what you'd run next with 30+ observations
(a second trial with news text via Alpaca MCP, and a stronger control).
